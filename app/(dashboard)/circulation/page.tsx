import { PlaceholderPage } from "@/components/dashboard/placeholder-page"

export default function CirculationPage() {
  return (
    <PlaceholderPage
      title="Circulation"
      description="Process loans and returns, enforce due dates, and guard against a copy being loaned to two patrons at once."
    />
  )
}